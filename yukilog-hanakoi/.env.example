# 后端 API 地址（SSR 环境使用，内网直连）
PUBLIC_API_URL=http://localhost:3639

# 站点公网地址（浏览器环境使用）
PUBLIC_SITE_URL=https://blog.yeastar.xin

# 开发模式：跳过管理端认证（仅开发时使用）
PUBLIC_DEV_SKIP_AUTH=false

# Mock 模式：使用本地 fixture 数据代替真实 API（本地无后端时使用）
PUBLIC_USE_MOCK=false
