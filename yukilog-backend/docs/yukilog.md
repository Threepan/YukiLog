<div align="center">

# YukiLog 设计文档

这里是设计文档, 会存放一些我的小叽里咕噜话

</div>

---

## 细腰模型

所谓 **细腰模型**，指的是一种非常自然、也几乎不可避免的软件结构形态：

> **两头规模巨大，中间高度收敛**

* **上端**：用户世界
页面、交互、审美、习惯、情绪、设备……复杂而不可控

* **下端**：底层基础设施
数据库、文件系统、网络、硬件、操作系统……复杂且冷酷

* **中间**：程序员精心雕刻出来的“腰部”
用抽象、接口和约定，把混沌压缩成秩序

不管是后端常说的 **面向对象 / 接口 / 分层**

还是前端熟悉的 **组件 / 路由 / 状态管理**

它们本质上都在做同一件事：

> **通过抽象与封装，把复杂世界压缩成可组合的简单接口**

用户的一句

> **「我要看这个页面」**

在系统内部, 会被层层翻译为:

> **请求数据 → 处理业务 → 组织结果 → 渲染界面**

**细腰模型不是设计出来的，而是复杂系统在长期演化中“长出来的形态”**

#### 细腰模型在 YukiLog 中的体现 🐾

```text
┌──────────────────────────────┐
│   现实世界 / 用户行为 / 输入     
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│        前端（UI / 状态）       
│    页面、交互、路由、组件树     
└──────────────┬───────────────┘
               │  JSON / HTTP
               ▼
┌──────────────────────────────┐
│     Handler 层（接口边界）        
│     · 参数校验                    
│     · 鉴权 / 鉴权失败即返回        
│     · Rust 类型 ⇄ JSON      
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│    Service 层（✨细腰核心✨）     
│    · 业务规则                    
│    · 用例编排                    
│    · “我要做什么”                
│    · 与 HTTP / DB 完全无关        
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│    Repo 层（领域数据访问）       
│    · 强类型 Domain Model         
│    · 封装 CRUD 原子操作          
│    · 隔离数据库实现细节           
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│    ORM 层（数据翻译）             
│    · SQL ⇄ Rust 类型             
│    · 行 → 结构体                 
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│    PostgreSQL                    
│    · 将复杂无序的数据             
│    · 整理为结构化、有约束的存储   
└──────────────────────────────┘

```

---

## 软删除

软删除的意思是, 不直接删除数据, 而是将数据标记为 "delete", 以后想要恢复数据, 只需要取消这个标记

> **Q: 为什么不做? 他会带来什么问题?**

因为这个业务说起来简单, 但复杂度其实非常高!

软删除的概念虽然是对删除的优化, 但底层实现其实是 **对删除的否定**

> **问题是："将 `status` = `deleted`" 这句话只在 数据库层 成立。**

一旦进入 **领域模型 / 服务层 / 查询层**，麻烦就开始指数级增长。

#### 简单列举几个麻烦

> **(1) 软删除在数据库的表现**

软删除的记录其实仍然存在于数据库中, 并且如果你的数据库写了 **UNIQUE** 约束, 就必须让数据库兼容软链接的处理 ...

于是你就会开始添加规则, 企图让数据库在检索时忽略那些 **delete**

* `UNIQUE (slug, status)`
* `WHERE status != 'deleted'`

> **结果会导致 `INDEX` `SELECT` `ORM` 复杂度直线上升, 全系统被迫要去维护一个幽灵条件**

> **(2) 无视架构越权操作**

> 软删除与状态流转操作，本质上是在做 创建 / 删除 一条记录
> 
>但它的逻辑又不是新建记录，只是修改一个列

一般操作一条记录, 一定会走完 `handler` -> `service` -> `repo` -> `orm` -> `db` 整个生命周期

但软删除的逻辑只是 `status` = `active`, 他完整的绕过了所有 `级联逻辑` `领域事件`

> **如果要为他单独写一套验证逻辑, 为什么不直接重新创建呢 ?**

---

## JWT 认证中间件

他的概念是: 在请求处理流程中拦截请求, 在请求到达 `handler` 之前/或之后 执行特定逻辑

这么说还是太枯燥了, 让我列举一个 **请求模型**, 然后用实际例子告诉你: 他在做什么

> **首先我们要明白, 一次请求在表层的模型可以简化为 `客户端` -> `handler层` -> `回到客户端`**

```text
客户端请求
    ↓
[中间件 1: CORS]        ← 检查跨域
    ↓
[中间件 2: 日志]         ← 记录请求
    ↓
[中间件 3: JWT 认证]     ← 验证令牌（我们要实现的）
    ↓
[Handler 函数]          ← 业务处理（如 create_post）
    ↓
[中间件响应处理]         ← 可以修改响应
    ↓
客户端收到响应
```

你感受到了吗? JWT 其实就是在做一些必要的验证逻辑, 保证数据在进入后端之前就能 "变得无害"

> **接下来我讲解一下 JWT 认证中间件到底做了什么**

1. **提取 `Authorization Header`**

`Header` 就是我们经常说的 **请求头**, 它包含了很多信息: 我是谁, 我带了什么, 我希望被如何对待

> 科普: **请求体 `Body`** 则包含了实际的业务数据

**这是一个真实的请求头:**

```bash
GET /api/posts HTTP/1.1                             # 请求类型/路由/协议
Host: api.yukilog.com                               # 请求地址
Authorization: Bearer eyJhbGciOiJIUzI1N...          # 给 JWT 看的通信证
User-Agent: Mozilla/5.0 (wayland; Linux x86_64)     # 我是谁?
Content-Type: application/json                      # 我发送的格式
Accept: application/json                            # 我期望的格式
Origin: https://yukilog.com                         # 我从哪儿来
```

**JWT 认证中间件** 提取的就是 `Authorization` 那一行

2. **检查格式**

检查 `Bearer <token>` 格式，然后决定继续验证，或者返回 **401 Unauthorized JSON 响应**

3. **验证 Token**

这里使用 `JWT_SECRET` 验证签名和过期时间，就是你在 [.env](../.env.example) 里配置的那一个

要么解析出 `Claims`，要么返回 **401 Unauthorized JSON 响应**，并区分错误类型：

* **缺少令牌或格式错误** → `{ success: false, data: null, message: "缺少认证令牌或格式错误" }`
* **令牌签名无效** → `{ success: false, data: null, message: "认证令牌无效" }`
* **令牌已过期** → `{ success: false, data: null, message: "认证令牌已过期" }`

4. **将 `Claims` 存入 `Request Extensions`**

`Request Extensions` 就是一个临时信息体, 只在本次请求的生命周期内有效

5. **放行**

检查完毕~你可以去下一层啦!!!

#### 给你完整的举个例子!

```text
用户登录:
POST /api/admin/auth/login
Body: {"username": "admin", "password": "xxx"}
Response: {"success": true, "data": {"token": "eyJ...", "expires_in": 86400}}

↓ 用户保存 token 到本地

创建文章（需要认证）:
POST /api/admin/posts
Headers: 
  Authorization: Bearer eyJ...
  Content-Type: application/json
Body: {"title": "...", "content": "..."}

↓ 请求到达服务器

[中间件 jwt_auth]
1. 提取 "Bearer eyJ..."
2. 验证 token → 解析出 Claims {sub: "admin", exp: 1314520}
   （如果过期：返回 401 JSON: {"success": false, "message": "认证令牌已过期"}）
   （如果无效：返回 401 JSON: {"success": false, "message": "认证令牌无效"}）
3. req.extensions_mut().insert(claims)
4. next.run(req)

↓

[Handler create_post]
1. Extension<Claims> 提取出 claims
2. 知道是 admin 用户在操作
3. 调用 service 层创建文章

↓ 返回成功响应
```

---

## 抱怨

这里用来记录开发过程中遇到的问题, 还有猪鼻恋的臆想

---

#### (1) **`view_count` 要防刷吗**

这个字段是用来记录浏览量的, 主题, 标签, 文章都有他的身影

> **Q: 要是文章每次被访问都会增加浏览量好不好?**

当然好! 我希望自己的文章浏览量超级超级多!

但是 ... 不可以!!! 必须要做防刷

不然坏人直接写一个 `curl` 脚本多挂几个进程, 不就直接数值溢出把我打爆了吗?

> 我发现, 只要是涉及到 "和外界交互", 就免不了去讨论 "安全问题"
>
> 但是啊, 做安全真的好累的, 为了防住 「整个世界」 的恶意, 我 「一个人」 需要付出很多呢 ...

---
